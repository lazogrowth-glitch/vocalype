# Evals

Evaluation assets are kept here for repeatable offline quality checks.

- `parakeet/`: speech-to-text dataset manifests, reports, and archived experiment outputs
- `postprocess/`: post-processing benchmark comparisons and archived benchmark runs

Keep stable entrypoints and canonical comparison files near the top level.
Move one-off runs and old probes into `archive/` folders to avoid clutter.
