# Postprocess Evals

This folder keeps the current post-processing comparison snapshots close at hand.

- top-level `postprocess-benchmark-comparison-*.json`: current comparison views worth opening directly
- `archive/benchmarks/`: timestamped benchmark runs and older one-off snapshots
- `archive/probes/`: saved probe histories
- `archive/logs/`: local stdout/stderr logs from benchmark runs
