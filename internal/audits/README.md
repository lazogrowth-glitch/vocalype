# Audits

Local audit and evaluation artifacts live here.

- `screenshots/raw/`: screenshots captured during audits
- `screenshots/final/`: curated screenshots kept as final references
- `workdirs/parakeet/`: large temporary evaluation worktrees and downloaded test data

`workdirs/` is intentionally treated as disposable working material and is
ignored by Git via `.gitignore`.
