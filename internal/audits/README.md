# Audits

Local audit and evaluation artifacts live here.

- `audit-shots/`: screenshots captured during audits
- `final-audit-shots/`: curated screenshots kept as final references
- `tmp-parakeet-tests/`: large temporary evaluation worktrees and downloaded test data

`tmp-parakeet-tests/` is intentionally treated as disposable working material and
is ignored by Git via `.gitignore`.
