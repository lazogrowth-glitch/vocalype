# Frontend Source

React and TypeScript application code.

- `components/`: UI building blocks
- `hooks/`: reusable React hooks
- `stores/`: client-side state containers
- `lib/` and `utils/`: shared helpers
- `i18n/`: translation setup and locale files
- `overlay/`: overlay-specific UI
- `agent/`: assistant-facing frontend logic
